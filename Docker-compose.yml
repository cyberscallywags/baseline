services:
  fastapi:
    build: .
    container_name: fastapi-uv-demo
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
    environment:
      - UV_CACHE_DIR=/root/.cache/uv
    command: uv run uvicorn app.main:app --host 0.0.0.0 --port 8000